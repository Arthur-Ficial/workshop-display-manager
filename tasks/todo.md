# TODO

Generated by `/plan` 2026-05-05. Source: `tasks/plan.md`. Updated as work shipped.

## Active

(none — clean stopping point. Below are candidates for the next session.)

## Candidates for next session

- [ ] **Wire VIRTUAL sidebar section live** — same shape as PROFILES, but `controller.virtual.list()` returns ALL displays (no virtual-only filter today); needs a way to distinguish virtuals from real displays at the Kit level first. M-sized.
- [ ] **External-change pickup for profiles** — if `wdm save` runs in another terminal, GUI doesn't see it until restart. Either poll (2-s timer) or fswatch on `WDM_PROFILES_DIR`. S-sized.
- [ ] **Brightness slider in Inspector** — `controller.brightness(displayID:)` already returns nil-or-0..1; render a slider for built-in displays, refuse-message for externals (matches CLAUDE.md's "honest unsupported-path policy"). S-sized.
- [ ] **Doctor probe panel in Inspector** — show `wdm doctor probe <id>` output (mode, origin, main, rotation, mirror) when a display is selected. S-sized.
- [ ] **Live `wdm watch` event log** — bottom panel that streams display reconfiguration events via `controller.observeReconfigurations`. S-sized.
- [ ] **Make `make demo-arrange-pipe`** — codify the CLAUDE.md unix-pipe drag-to-rearrange demo as a runnable Makefile target. XS-sized.

## Done this session

### Lifecycle workflow run (commits aa59a6e → 904a1b3)
- [x] Install Addy Osmani agent-skills locally (`.claude/skills/`, `.claude/commands/`, `.claude/agents/`)
- [x] Generate SPEC.md + tasks/plan.md from `/spec` `/plan`
- [x] Lint rule: `Tests/WDMCLITests/DisabledTestsHaveLinkTest.swift` enforces `@Test(.disabled)` carries a `docs/known-flakes.md#anchor` link (3 sub-tests: project-tree compliant, scanner detects violations, anchors resolve to real headings)
- [x] `docs/known-flakes.md` — registry; `headed-settings-parallel` is the first entry

### PROFILES section feature-complete (commits d503861 → ce8f7e3)
- [x] PROFILES sidebar reads live `controller.profiles()` (replaces hardcoded `count: 0` + empty hint)
- [x] `WDMMacRemoteRunner` syncs profile rows into the headless registry (3-way `combineLatest` of tiles + selected + profiles)
- [x] Click profile row → `vm.restoreProfile(named:)` → `controller.restoreProfile` → fixture state mutates (e2e proof: read fixture file post-click, assert origin moved)
- [x] `+` button → `vm.saveCurrentAsProfile()` → timestamp-named profile + immediate row refresh
- [x] Updated `snapshotShowsFixtureDisplays` to filter to display IDs so future sidebar additions don't regress it

## Final state

- `swift test`: **477 / 116 green** (started at 470/114; net +7 tests, all real e2e)
- `swift build -c release -Xswiftc -warnings-as-errors`: clean
- `make lint-remote-coverage`: ✓ (4 clickable + 17 passive elements covered)
- 5 commits on `main`, each pre-commit-validated (lint + release build + hermetic tests)
