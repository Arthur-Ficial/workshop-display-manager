# Archive

This directory preserves retired code that is intentionally outside the active
SwiftPM package.

Current archive:

- [`gui/2026-05-09`](gui/2026-05-09/README.md) — the retired Mac GUI,
  remote-control companion, GUI tests, GUI scripts, and design docs.

Archived code is reference material only. Active builds, tests, releases, and
package products live under the repository root `Sources/`, `Tests/`, `scripts/`,
and `docs/` directories.
